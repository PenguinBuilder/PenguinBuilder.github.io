export const vars = {
    version: "v4.2.0",
    "whats-new": `
    small bug fixes and added to the expension API
    `.trim(),
}


export const helpers = {
    or(a: any, b: any) {
        return a ?? b;
    }
}
